# Opcional: solo si se usa el fallback football-data.org
FOOTBALL_DATA_API_KEY=

# Secreto compartido para proteger POST /ingest (webhook de entrada). Opcional.
INGEST_SECRET=

# Cadencia de sondeo (segundos). 2 s en vivo para apuntar a <3 s de pipeline.
POLL_INTERVAL_LIVE=2
POLL_INTERVAL_IDLE=60
